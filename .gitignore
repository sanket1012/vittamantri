# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.log

# Environment secrets — NEVER commit these
backend/.env
.env
.env.local
.env.*.local

# Frontend
frontend/node_modules/
frontend/dist/

# Data files (contain personal financial records)
data/transactions.csv
data/transactions_old.csv
data/summary.json

# OS
.DS_Store
Thumbs.db

# Fly.io / Render (not used)
fly.toml
render.yaml

# EC2 deploy — virtual env lives on the server, not in repo
.venv/
